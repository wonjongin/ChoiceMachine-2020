import React, {Component} from 'react';
import {
  View,
  Text,
  Button,
  TouchableOpacity,
  StyleSheet,
  Platform,
} from 'react-native';

function BlindBox({navigation}) {
  return <Text>hi</Text>;
}

export default BlindBox;
